// dynamically typed languages allow us to not strictly declare what type of variable
var a = 100;
// vs:
// int a; a = 100;

// in dynamically typed languages, type checking during "runtime".

// dynamically vs static ! controversial topic xD

// TypeScript is the static form of JavaScript.
